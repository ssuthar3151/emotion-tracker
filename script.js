// emotion-tracker JavaScript functionality

// Function to track emotions 
function trackEmotion(emotion) {
    const timestamp = new Date().toISOString();
    console.log(`Emotion tracked: ${emotion} at ${timestamp}`);
    // You can store the emotion with the timestamp in a database or local storage
}

// Example usage: trackEmotion('happy');
